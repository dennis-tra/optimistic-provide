// Code generated by SQLBoiler 4.6.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// Provide is an object representing the database table.
type Provide struct {
	ID                    int         `boil:"id" json:"id" toml:"id" yaml:"id"`
	ProvideType           string      `boil:"provide_type" json:"provide_type" toml:"provide_type" yaml:"provide_type"`
	ProviderID            int         `boil:"provider_id" json:"provider_id" toml:"provider_id" yaml:"provider_id"`
	ContentID             string      `boil:"content_id" json:"content_id" toml:"content_id" yaml:"content_id"`
	Distance              []byte      `boil:"distance" json:"distance" toml:"distance" yaml:"distance"`
	InitialRoutingTableID int         `boil:"initial_routing_table_id" json:"initial_routing_table_id" toml:"initial_routing_table_id" yaml:"initial_routing_table_id"`
	FinalRoutingTableID   null.Int    `boil:"final_routing_table_id" json:"final_routing_table_id,omitempty" toml:"final_routing_table_id" yaml:"final_routing_table_id,omitempty"`
	StartedAt             time.Time   `boil:"started_at" json:"started_at" toml:"started_at" yaml:"started_at"`
	EndedAt               null.Time   `boil:"ended_at" json:"ended_at,omitempty" toml:"ended_at" yaml:"ended_at,omitempty"`
	Error                 null.String `boil:"error" json:"error,omitempty" toml:"error" yaml:"error,omitempty"`
	DoneAt                null.Time   `boil:"done_at" json:"done_at,omitempty" toml:"done_at" yaml:"done_at,omitempty"`
	UpdatedAt             time.Time   `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	CreatedAt             time.Time   `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`

	R *provideR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L provideL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var ProvideColumns = struct {
	ID                    string
	ProvideType           string
	ProviderID            string
	ContentID             string
	Distance              string
	InitialRoutingTableID string
	FinalRoutingTableID   string
	StartedAt             string
	EndedAt               string
	Error                 string
	DoneAt                string
	UpdatedAt             string
	CreatedAt             string
}{
	ID:                    "id",
	ProvideType:           "provide_type",
	ProviderID:            "provider_id",
	ContentID:             "content_id",
	Distance:              "distance",
	InitialRoutingTableID: "initial_routing_table_id",
	FinalRoutingTableID:   "final_routing_table_id",
	StartedAt:             "started_at",
	EndedAt:               "ended_at",
	Error:                 "error",
	DoneAt:                "done_at",
	UpdatedAt:             "updated_at",
	CreatedAt:             "created_at",
}

var ProvideTableColumns = struct {
	ID                    string
	ProvideType           string
	ProviderID            string
	ContentID             string
	Distance              string
	InitialRoutingTableID string
	FinalRoutingTableID   string
	StartedAt             string
	EndedAt               string
	Error                 string
	DoneAt                string
	UpdatedAt             string
	CreatedAt             string
}{
	ID:                    "provides.id",
	ProvideType:           "provides.provide_type",
	ProviderID:            "provides.provider_id",
	ContentID:             "provides.content_id",
	Distance:              "provides.distance",
	InitialRoutingTableID: "provides.initial_routing_table_id",
	FinalRoutingTableID:   "provides.final_routing_table_id",
	StartedAt:             "provides.started_at",
	EndedAt:               "provides.ended_at",
	Error:                 "provides.error",
	DoneAt:                "provides.done_at",
	UpdatedAt:             "provides.updated_at",
	CreatedAt:             "provides.created_at",
}

// Generated where

var ProvideWhere = struct {
	ID                    whereHelperint
	ProvideType           whereHelperstring
	ProviderID            whereHelperint
	ContentID             whereHelperstring
	Distance              whereHelper__byte
	InitialRoutingTableID whereHelperint
	FinalRoutingTableID   whereHelpernull_Int
	StartedAt             whereHelpertime_Time
	EndedAt               whereHelpernull_Time
	Error                 whereHelpernull_String
	DoneAt                whereHelpernull_Time
	UpdatedAt             whereHelpertime_Time
	CreatedAt             whereHelpertime_Time
}{
	ID:                    whereHelperint{field: "\"provides\".\"id\""},
	ProvideType:           whereHelperstring{field: "\"provides\".\"provide_type\""},
	ProviderID:            whereHelperint{field: "\"provides\".\"provider_id\""},
	ContentID:             whereHelperstring{field: "\"provides\".\"content_id\""},
	Distance:              whereHelper__byte{field: "\"provides\".\"distance\""},
	InitialRoutingTableID: whereHelperint{field: "\"provides\".\"initial_routing_table_id\""},
	FinalRoutingTableID:   whereHelpernull_Int{field: "\"provides\".\"final_routing_table_id\""},
	StartedAt:             whereHelpertime_Time{field: "\"provides\".\"started_at\""},
	EndedAt:               whereHelpernull_Time{field: "\"provides\".\"ended_at\""},
	Error:                 whereHelpernull_String{field: "\"provides\".\"error\""},
	DoneAt:                whereHelpernull_Time{field: "\"provides\".\"done_at\""},
	UpdatedAt:             whereHelpertime_Time{field: "\"provides\".\"updated_at\""},
	CreatedAt:             whereHelpertime_Time{field: "\"provides\".\"created_at\""},
}

// ProvideRels is where relationship names are stored.
var ProvideRels = struct {
	FinalRoutingTable   string
	InitialRoutingTable string
	Provider            string
	AddProviderRPCS     string
	Connections         string
	Dials               string
	FindNodesRPCS       string
	PeerStates          string
}{
	FinalRoutingTable:   "FinalRoutingTable",
	InitialRoutingTable: "InitialRoutingTable",
	Provider:            "Provider",
	AddProviderRPCS:     "AddProviderRPCS",
	Connections:         "Connections",
	Dials:               "Dials",
	FindNodesRPCS:       "FindNodesRPCS",
	PeerStates:          "PeerStates",
}

// provideR is where relationships are stored.
type provideR struct {
	FinalRoutingTable   *RoutingTableSnapshot `boil:"FinalRoutingTable" json:"FinalRoutingTable" toml:"FinalRoutingTable" yaml:"FinalRoutingTable"`
	InitialRoutingTable *RoutingTableSnapshot `boil:"InitialRoutingTable" json:"InitialRoutingTable" toml:"InitialRoutingTable" yaml:"InitialRoutingTable"`
	Provider            *Peer                 `boil:"Provider" json:"Provider" toml:"Provider" yaml:"Provider"`
	AddProviderRPCS     AddProviderRPCSlice   `boil:"AddProviderRPCS" json:"AddProviderRPCS" toml:"AddProviderRPCS" yaml:"AddProviderRPCS"`
	Connections         ConnectionSlice       `boil:"Connections" json:"Connections" toml:"Connections" yaml:"Connections"`
	Dials               DialSlice             `boil:"Dials" json:"Dials" toml:"Dials" yaml:"Dials"`
	FindNodesRPCS       FindNodesRPCSlice     `boil:"FindNodesRPCS" json:"FindNodesRPCS" toml:"FindNodesRPCS" yaml:"FindNodesRPCS"`
	PeerStates          PeerStateSlice        `boil:"PeerStates" json:"PeerStates" toml:"PeerStates" yaml:"PeerStates"`
}

// NewStruct creates a new relationship struct
func (*provideR) NewStruct() *provideR {
	return &provideR{}
}

// provideL is where Load methods for each relationship are stored.
type provideL struct{}

var (
	provideAllColumns            = []string{"id", "provide_type", "provider_id", "content_id", "distance", "initial_routing_table_id", "final_routing_table_id", "started_at", "ended_at", "error", "done_at", "updated_at", "created_at"}
	provideColumnsWithoutDefault = []string{"provide_type", "provider_id", "content_id", "distance", "initial_routing_table_id", "final_routing_table_id", "started_at", "ended_at", "error", "done_at", "updated_at", "created_at"}
	provideColumnsWithDefault    = []string{"id"}
	providePrimaryKeyColumns     = []string{"id"}
)

type (
	// ProvideSlice is an alias for a slice of pointers to Provide.
	// This should almost always be used instead of []Provide.
	ProvideSlice []*Provide
	// ProvideHook is the signature for custom Provide hook methods
	ProvideHook func(context.Context, boil.ContextExecutor, *Provide) error

	provideQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	provideType                 = reflect.TypeOf(&Provide{})
	provideMapping              = queries.MakeStructMapping(provideType)
	providePrimaryKeyMapping, _ = queries.BindMapping(provideType, provideMapping, providePrimaryKeyColumns)
	provideInsertCacheMut       sync.RWMutex
	provideInsertCache          = make(map[string]insertCache)
	provideUpdateCacheMut       sync.RWMutex
	provideUpdateCache          = make(map[string]updateCache)
	provideUpsertCacheMut       sync.RWMutex
	provideUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var provideBeforeInsertHooks []ProvideHook
var provideBeforeUpdateHooks []ProvideHook
var provideBeforeDeleteHooks []ProvideHook
var provideBeforeUpsertHooks []ProvideHook

var provideAfterInsertHooks []ProvideHook
var provideAfterSelectHooks []ProvideHook
var provideAfterUpdateHooks []ProvideHook
var provideAfterDeleteHooks []ProvideHook
var provideAfterUpsertHooks []ProvideHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *Provide) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range provideBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *Provide) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range provideBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *Provide) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range provideBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *Provide) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range provideBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *Provide) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range provideAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *Provide) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range provideAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *Provide) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range provideAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *Provide) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range provideAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *Provide) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range provideAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddProvideHook registers your hook function for all future operations.
func AddProvideHook(hookPoint boil.HookPoint, provideHook ProvideHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		provideBeforeInsertHooks = append(provideBeforeInsertHooks, provideHook)
	case boil.BeforeUpdateHook:
		provideBeforeUpdateHooks = append(provideBeforeUpdateHooks, provideHook)
	case boil.BeforeDeleteHook:
		provideBeforeDeleteHooks = append(provideBeforeDeleteHooks, provideHook)
	case boil.BeforeUpsertHook:
		provideBeforeUpsertHooks = append(provideBeforeUpsertHooks, provideHook)
	case boil.AfterInsertHook:
		provideAfterInsertHooks = append(provideAfterInsertHooks, provideHook)
	case boil.AfterSelectHook:
		provideAfterSelectHooks = append(provideAfterSelectHooks, provideHook)
	case boil.AfterUpdateHook:
		provideAfterUpdateHooks = append(provideAfterUpdateHooks, provideHook)
	case boil.AfterDeleteHook:
		provideAfterDeleteHooks = append(provideAfterDeleteHooks, provideHook)
	case boil.AfterUpsertHook:
		provideAfterUpsertHooks = append(provideAfterUpsertHooks, provideHook)
	}
}

// One returns a single provide record from the query.
func (q provideQuery) One(ctx context.Context, exec boil.ContextExecutor) (*Provide, error) {
	o := &Provide{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for provides")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all Provide records from the query.
func (q provideQuery) All(ctx context.Context, exec boil.ContextExecutor) (ProvideSlice, error) {
	var o []*Provide

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to Provide slice")
	}

	if len(provideAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all Provide records in the query.
func (q provideQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count provides rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q provideQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if provides exists")
	}

	return count > 0, nil
}

// FinalRoutingTable pointed to by the foreign key.
func (o *Provide) FinalRoutingTable(mods ...qm.QueryMod) routingTableSnapshotQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.FinalRoutingTableID),
	}

	queryMods = append(queryMods, mods...)

	query := RoutingTableSnapshots(queryMods...)
	queries.SetFrom(query.Query, "\"routing_table_snapshots\"")

	return query
}

// InitialRoutingTable pointed to by the foreign key.
func (o *Provide) InitialRoutingTable(mods ...qm.QueryMod) routingTableSnapshotQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.InitialRoutingTableID),
	}

	queryMods = append(queryMods, mods...)

	query := RoutingTableSnapshots(queryMods...)
	queries.SetFrom(query.Query, "\"routing_table_snapshots\"")

	return query
}

// Provider pointed to by the foreign key.
func (o *Provide) Provider(mods ...qm.QueryMod) peerQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.ProviderID),
	}

	queryMods = append(queryMods, mods...)

	query := Peers(queryMods...)
	queries.SetFrom(query.Query, "\"peers\"")

	return query
}

// AddProviderRPCS retrieves all the add_provider_rpc's AddProviderRPCS with an executor.
func (o *Provide) AddProviderRPCS(mods ...qm.QueryMod) addProviderRPCQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.InnerJoin("\"provides_x_add_provider_rpcs\" on \"add_provider_rpcs\".\"id\" = \"provides_x_add_provider_rpcs\".\"add_provider_rpc_id\""),
		qm.Where("\"provides_x_add_provider_rpcs\".\"provide_id\"=?", o.ID),
	)

	query := AddProviderRPCS(queryMods...)
	queries.SetFrom(query.Query, "\"add_provider_rpcs\"")

	if len(queries.GetSelect(query.Query)) == 0 {
		queries.SetSelect(query.Query, []string{"\"add_provider_rpcs\".*"})
	}

	return query
}

// Connections retrieves all the connection's Connections with an executor.
func (o *Provide) Connections(mods ...qm.QueryMod) connectionQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.InnerJoin("\"provides_x_connections\" on \"connections\".\"id\" = \"provides_x_connections\".\"connection_id\""),
		qm.Where("\"provides_x_connections\".\"provide_id\"=?", o.ID),
	)

	query := Connections(queryMods...)
	queries.SetFrom(query.Query, "\"connections\"")

	if len(queries.GetSelect(query.Query)) == 0 {
		queries.SetSelect(query.Query, []string{"\"connections\".*"})
	}

	return query
}

// Dials retrieves all the dial's Dials with an executor.
func (o *Provide) Dials(mods ...qm.QueryMod) dialQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.InnerJoin("\"provides_x_dials\" on \"dials\".\"id\" = \"provides_x_dials\".\"dial_id\""),
		qm.Where("\"provides_x_dials\".\"provide_id\"=?", o.ID),
	)

	query := Dials(queryMods...)
	queries.SetFrom(query.Query, "\"dials\"")

	if len(queries.GetSelect(query.Query)) == 0 {
		queries.SetSelect(query.Query, []string{"\"dials\".*"})
	}

	return query
}

// FindNodesRPCS retrieves all the find_nodes_rpc's FindNodesRPCS with an executor.
func (o *Provide) FindNodesRPCS(mods ...qm.QueryMod) findNodesRPCQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.InnerJoin("\"provides_x_find_nodes_rpcs\" on \"find_nodes_rpcs\".\"id\" = \"provides_x_find_nodes_rpcs\".\"find_nodes_rpc_id\""),
		qm.Where("\"provides_x_find_nodes_rpcs\".\"provide_id\"=?", o.ID),
	)

	query := FindNodesRPCS(queryMods...)
	queries.SetFrom(query.Query, "\"find_nodes_rpcs\"")

	if len(queries.GetSelect(query.Query)) == 0 {
		queries.SetSelect(query.Query, []string{"\"find_nodes_rpcs\".*"})
	}

	return query
}

// PeerStates retrieves all the peer_state's PeerStates with an executor.
func (o *Provide) PeerStates(mods ...qm.QueryMod) peerStateQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.InnerJoin("\"provides_x_peer_states\" on \"peer_states\".\"id\" = \"provides_x_peer_states\".\"peer_state_id\""),
		qm.Where("\"provides_x_peer_states\".\"provide_id\"=?", o.ID),
	)

	query := PeerStates(queryMods...)
	queries.SetFrom(query.Query, "\"peer_states\"")

	if len(queries.GetSelect(query.Query)) == 0 {
		queries.SetSelect(query.Query, []string{"\"peer_states\".*"})
	}

	return query
}

// LoadFinalRoutingTable allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (provideL) LoadFinalRoutingTable(ctx context.Context, e boil.ContextExecutor, singular bool, maybeProvide interface{}, mods queries.Applicator) error {
	var slice []*Provide
	var object *Provide

	if singular {
		object = maybeProvide.(*Provide)
	} else {
		slice = *maybeProvide.(*[]*Provide)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &provideR{}
		}
		if !queries.IsNil(object.FinalRoutingTableID) {
			args = append(args, object.FinalRoutingTableID)
		}

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &provideR{}
			}

			for _, a := range args {
				if queries.Equal(a, obj.FinalRoutingTableID) {
					continue Outer
				}
			}

			if !queries.IsNil(obj.FinalRoutingTableID) {
				args = append(args, obj.FinalRoutingTableID)
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`routing_table_snapshots`),
		qm.WhereIn(`routing_table_snapshots.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load RoutingTableSnapshot")
	}

	var resultSlice []*RoutingTableSnapshot
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice RoutingTableSnapshot")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for routing_table_snapshots")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for routing_table_snapshots")
	}

	if len(provideAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.FinalRoutingTable = foreign
		if foreign.R == nil {
			foreign.R = &routingTableSnapshotR{}
		}
		foreign.R.FinalRoutingTableProvides = append(foreign.R.FinalRoutingTableProvides, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.FinalRoutingTableID, foreign.ID) {
				local.R.FinalRoutingTable = foreign
				if foreign.R == nil {
					foreign.R = &routingTableSnapshotR{}
				}
				foreign.R.FinalRoutingTableProvides = append(foreign.R.FinalRoutingTableProvides, local)
				break
			}
		}
	}

	return nil
}

// LoadInitialRoutingTable allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (provideL) LoadInitialRoutingTable(ctx context.Context, e boil.ContextExecutor, singular bool, maybeProvide interface{}, mods queries.Applicator) error {
	var slice []*Provide
	var object *Provide

	if singular {
		object = maybeProvide.(*Provide)
	} else {
		slice = *maybeProvide.(*[]*Provide)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &provideR{}
		}
		args = append(args, object.InitialRoutingTableID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &provideR{}
			}

			for _, a := range args {
				if a == obj.InitialRoutingTableID {
					continue Outer
				}
			}

			args = append(args, obj.InitialRoutingTableID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`routing_table_snapshots`),
		qm.WhereIn(`routing_table_snapshots.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load RoutingTableSnapshot")
	}

	var resultSlice []*RoutingTableSnapshot
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice RoutingTableSnapshot")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for routing_table_snapshots")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for routing_table_snapshots")
	}

	if len(provideAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.InitialRoutingTable = foreign
		if foreign.R == nil {
			foreign.R = &routingTableSnapshotR{}
		}
		foreign.R.InitialRoutingTableProvides = append(foreign.R.InitialRoutingTableProvides, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.InitialRoutingTableID == foreign.ID {
				local.R.InitialRoutingTable = foreign
				if foreign.R == nil {
					foreign.R = &routingTableSnapshotR{}
				}
				foreign.R.InitialRoutingTableProvides = append(foreign.R.InitialRoutingTableProvides, local)
				break
			}
		}
	}

	return nil
}

// LoadProvider allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (provideL) LoadProvider(ctx context.Context, e boil.ContextExecutor, singular bool, maybeProvide interface{}, mods queries.Applicator) error {
	var slice []*Provide
	var object *Provide

	if singular {
		object = maybeProvide.(*Provide)
	} else {
		slice = *maybeProvide.(*[]*Provide)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &provideR{}
		}
		args = append(args, object.ProviderID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &provideR{}
			}

			for _, a := range args {
				if a == obj.ProviderID {
					continue Outer
				}
			}

			args = append(args, obj.ProviderID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`peers`),
		qm.WhereIn(`peers.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Peer")
	}

	var resultSlice []*Peer
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Peer")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for peers")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for peers")
	}

	if len(provideAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Provider = foreign
		if foreign.R == nil {
			foreign.R = &peerR{}
		}
		foreign.R.ProviderProvides = append(foreign.R.ProviderProvides, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ProviderID == foreign.ID {
				local.R.Provider = foreign
				if foreign.R == nil {
					foreign.R = &peerR{}
				}
				foreign.R.ProviderProvides = append(foreign.R.ProviderProvides, local)
				break
			}
		}
	}

	return nil
}

// LoadAddProviderRPCS allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (provideL) LoadAddProviderRPCS(ctx context.Context, e boil.ContextExecutor, singular bool, maybeProvide interface{}, mods queries.Applicator) error {
	var slice []*Provide
	var object *Provide

	if singular {
		object = maybeProvide.(*Provide)
	} else {
		slice = *maybeProvide.(*[]*Provide)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &provideR{}
		}
		args = append(args, object.ID)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &provideR{}
			}

			for _, a := range args {
				if a == obj.ID {
					continue Outer
				}
			}

			args = append(args, obj.ID)
		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.Select("\"add_provider_rpcs\".id, \"add_provider_rpcs\".local_id, \"add_provider_rpcs\".remote_id, \"add_provider_rpcs\".distance, \"add_provider_rpcs\".multi_address_ids, \"add_provider_rpcs\".started_at, \"add_provider_rpcs\".ended_at, \"add_provider_rpcs\".error, \"a\".\"provide_id\""),
		qm.From("\"add_provider_rpcs\""),
		qm.InnerJoin("\"provides_x_add_provider_rpcs\" as \"a\" on \"add_provider_rpcs\".\"id\" = \"a\".\"add_provider_rpc_id\""),
		qm.WhereIn("\"a\".\"provide_id\" in ?", args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load add_provider_rpcs")
	}

	var resultSlice []*AddProviderRPC

	var localJoinCols []int
	for results.Next() {
		one := new(AddProviderRPC)
		var localJoinCol int

		err = results.Scan(&one.ID, &one.LocalID, &one.RemoteID, &one.Distance, &one.MultiAddressIds, &one.StartedAt, &one.EndedAt, &one.Error, &localJoinCol)
		if err != nil {
			return errors.Wrap(err, "failed to scan eager loaded results for add_provider_rpcs")
		}
		if err = results.Err(); err != nil {
			return errors.Wrap(err, "failed to plebian-bind eager loaded slice add_provider_rpcs")
		}

		resultSlice = append(resultSlice, one)
		localJoinCols = append(localJoinCols, localJoinCol)
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on add_provider_rpcs")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for add_provider_rpcs")
	}

	if len(addProviderRPCAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.AddProviderRPCS = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &addProviderRPCR{}
			}
			foreign.R.Provides = append(foreign.R.Provides, object)
		}
		return nil
	}

	for i, foreign := range resultSlice {
		localJoinCol := localJoinCols[i]
		for _, local := range slice {
			if local.ID == localJoinCol {
				local.R.AddProviderRPCS = append(local.R.AddProviderRPCS, foreign)
				if foreign.R == nil {
					foreign.R = &addProviderRPCR{}
				}
				foreign.R.Provides = append(foreign.R.Provides, local)
				break
			}
		}
	}

	return nil
}

// LoadConnections allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (provideL) LoadConnections(ctx context.Context, e boil.ContextExecutor, singular bool, maybeProvide interface{}, mods queries.Applicator) error {
	var slice []*Provide
	var object *Provide

	if singular {
		object = maybeProvide.(*Provide)
	} else {
		slice = *maybeProvide.(*[]*Provide)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &provideR{}
		}
		args = append(args, object.ID)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &provideR{}
			}

			for _, a := range args {
				if a == obj.ID {
					continue Outer
				}
			}

			args = append(args, obj.ID)
		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.Select("\"connections\".id, \"connections\".local_id, \"connections\".remote_id, \"connections\".multi_address_id, \"connections\".started_at, \"connections\".ended_at, \"a\".\"provide_id\""),
		qm.From("\"connections\""),
		qm.InnerJoin("\"provides_x_connections\" as \"a\" on \"connections\".\"id\" = \"a\".\"connection_id\""),
		qm.WhereIn("\"a\".\"provide_id\" in ?", args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load connections")
	}

	var resultSlice []*Connection

	var localJoinCols []int
	for results.Next() {
		one := new(Connection)
		var localJoinCol int

		err = results.Scan(&one.ID, &one.LocalID, &one.RemoteID, &one.MultiAddressID, &one.StartedAt, &one.EndedAt, &localJoinCol)
		if err != nil {
			return errors.Wrap(err, "failed to scan eager loaded results for connections")
		}
		if err = results.Err(); err != nil {
			return errors.Wrap(err, "failed to plebian-bind eager loaded slice connections")
		}

		resultSlice = append(resultSlice, one)
		localJoinCols = append(localJoinCols, localJoinCol)
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on connections")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for connections")
	}

	if len(connectionAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.Connections = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &connectionR{}
			}
			foreign.R.Provides = append(foreign.R.Provides, object)
		}
		return nil
	}

	for i, foreign := range resultSlice {
		localJoinCol := localJoinCols[i]
		for _, local := range slice {
			if local.ID == localJoinCol {
				local.R.Connections = append(local.R.Connections, foreign)
				if foreign.R == nil {
					foreign.R = &connectionR{}
				}
				foreign.R.Provides = append(foreign.R.Provides, local)
				break
			}
		}
	}

	return nil
}

// LoadDials allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (provideL) LoadDials(ctx context.Context, e boil.ContextExecutor, singular bool, maybeProvide interface{}, mods queries.Applicator) error {
	var slice []*Provide
	var object *Provide

	if singular {
		object = maybeProvide.(*Provide)
	} else {
		slice = *maybeProvide.(*[]*Provide)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &provideR{}
		}
		args = append(args, object.ID)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &provideR{}
			}

			for _, a := range args {
				if a == obj.ID {
					continue Outer
				}
			}

			args = append(args, obj.ID)
		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.Select("\"dials\".id, \"dials\".local_id, \"dials\".remote_id, \"dials\".transport, \"dials\".multi_address_id, \"dials\".started_at, \"dials\".ended_at, \"dials\".error, \"a\".\"provide_id\""),
		qm.From("\"dials\""),
		qm.InnerJoin("\"provides_x_dials\" as \"a\" on \"dials\".\"id\" = \"a\".\"dial_id\""),
		qm.WhereIn("\"a\".\"provide_id\" in ?", args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load dials")
	}

	var resultSlice []*Dial

	var localJoinCols []int
	for results.Next() {
		one := new(Dial)
		var localJoinCol int

		err = results.Scan(&one.ID, &one.LocalID, &one.RemoteID, &one.Transport, &one.MultiAddressID, &one.StartedAt, &one.EndedAt, &one.Error, &localJoinCol)
		if err != nil {
			return errors.Wrap(err, "failed to scan eager loaded results for dials")
		}
		if err = results.Err(); err != nil {
			return errors.Wrap(err, "failed to plebian-bind eager loaded slice dials")
		}

		resultSlice = append(resultSlice, one)
		localJoinCols = append(localJoinCols, localJoinCol)
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on dials")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for dials")
	}

	if len(dialAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.Dials = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &dialR{}
			}
			foreign.R.Provides = append(foreign.R.Provides, object)
		}
		return nil
	}

	for i, foreign := range resultSlice {
		localJoinCol := localJoinCols[i]
		for _, local := range slice {
			if local.ID == localJoinCol {
				local.R.Dials = append(local.R.Dials, foreign)
				if foreign.R == nil {
					foreign.R = &dialR{}
				}
				foreign.R.Provides = append(foreign.R.Provides, local)
				break
			}
		}
	}

	return nil
}

// LoadFindNodesRPCS allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (provideL) LoadFindNodesRPCS(ctx context.Context, e boil.ContextExecutor, singular bool, maybeProvide interface{}, mods queries.Applicator) error {
	var slice []*Provide
	var object *Provide

	if singular {
		object = maybeProvide.(*Provide)
	} else {
		slice = *maybeProvide.(*[]*Provide)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &provideR{}
		}
		args = append(args, object.ID)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &provideR{}
			}

			for _, a := range args {
				if a == obj.ID {
					continue Outer
				}
			}

			args = append(args, obj.ID)
		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.Select("\"find_nodes_rpcs\".id, \"find_nodes_rpcs\".query_id, \"find_nodes_rpcs\".local_id, \"find_nodes_rpcs\".remote_id, \"find_nodes_rpcs\".started_at, \"find_nodes_rpcs\".ended_at, \"find_nodes_rpcs\".closer_peers_count, \"find_nodes_rpcs\".error, \"a\".\"provide_id\""),
		qm.From("\"find_nodes_rpcs\""),
		qm.InnerJoin("\"provides_x_find_nodes_rpcs\" as \"a\" on \"find_nodes_rpcs\".\"id\" = \"a\".\"find_nodes_rpc_id\""),
		qm.WhereIn("\"a\".\"provide_id\" in ?", args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load find_nodes_rpcs")
	}

	var resultSlice []*FindNodesRPC

	var localJoinCols []int
	for results.Next() {
		one := new(FindNodesRPC)
		var localJoinCol int

		err = results.Scan(&one.ID, &one.QueryID, &one.LocalID, &one.RemoteID, &one.StartedAt, &one.EndedAt, &one.CloserPeersCount, &one.Error, &localJoinCol)
		if err != nil {
			return errors.Wrap(err, "failed to scan eager loaded results for find_nodes_rpcs")
		}
		if err = results.Err(); err != nil {
			return errors.Wrap(err, "failed to plebian-bind eager loaded slice find_nodes_rpcs")
		}

		resultSlice = append(resultSlice, one)
		localJoinCols = append(localJoinCols, localJoinCol)
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on find_nodes_rpcs")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for find_nodes_rpcs")
	}

	if len(findNodesRPCAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.FindNodesRPCS = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &findNodesRPCR{}
			}
			foreign.R.Provides = append(foreign.R.Provides, object)
		}
		return nil
	}

	for i, foreign := range resultSlice {
		localJoinCol := localJoinCols[i]
		for _, local := range slice {
			if local.ID == localJoinCol {
				local.R.FindNodesRPCS = append(local.R.FindNodesRPCS, foreign)
				if foreign.R == nil {
					foreign.R = &findNodesRPCR{}
				}
				foreign.R.Provides = append(foreign.R.Provides, local)
				break
			}
		}
	}

	return nil
}

// LoadPeerStates allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (provideL) LoadPeerStates(ctx context.Context, e boil.ContextExecutor, singular bool, maybeProvide interface{}, mods queries.Applicator) error {
	var slice []*Provide
	var object *Provide

	if singular {
		object = maybeProvide.(*Provide)
	} else {
		slice = *maybeProvide.(*[]*Provide)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &provideR{}
		}
		args = append(args, object.ID)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &provideR{}
			}

			for _, a := range args {
				if a == obj.ID {
					continue Outer
				}
			}

			args = append(args, obj.ID)
		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.Select("\"peer_states\".id, \"peer_states\".query_id, \"peer_states\".peer_id, \"peer_states\".referrer_id, \"peer_states\".state, \"peer_states\".distance, \"a\".\"provide_id\""),
		qm.From("\"peer_states\""),
		qm.InnerJoin("\"provides_x_peer_states\" as \"a\" on \"peer_states\".\"id\" = \"a\".\"peer_state_id\""),
		qm.WhereIn("\"a\".\"provide_id\" in ?", args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load peer_states")
	}

	var resultSlice []*PeerState

	var localJoinCols []int
	for results.Next() {
		one := new(PeerState)
		var localJoinCol int

		err = results.Scan(&one.ID, &one.QueryID, &one.PeerID, &one.ReferrerID, &one.State, &one.Distance, &localJoinCol)
		if err != nil {
			return errors.Wrap(err, "failed to scan eager loaded results for peer_states")
		}
		if err = results.Err(); err != nil {
			return errors.Wrap(err, "failed to plebian-bind eager loaded slice peer_states")
		}

		resultSlice = append(resultSlice, one)
		localJoinCols = append(localJoinCols, localJoinCol)
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on peer_states")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for peer_states")
	}

	if len(peerStateAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.PeerStates = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &peerStateR{}
			}
			foreign.R.Provides = append(foreign.R.Provides, object)
		}
		return nil
	}

	for i, foreign := range resultSlice {
		localJoinCol := localJoinCols[i]
		for _, local := range slice {
			if local.ID == localJoinCol {
				local.R.PeerStates = append(local.R.PeerStates, foreign)
				if foreign.R == nil {
					foreign.R = &peerStateR{}
				}
				foreign.R.Provides = append(foreign.R.Provides, local)
				break
			}
		}
	}

	return nil
}

// SetFinalRoutingTable of the provide to the related item.
// Sets o.R.FinalRoutingTable to related.
// Adds o to related.R.FinalRoutingTableProvides.
func (o *Provide) SetFinalRoutingTable(ctx context.Context, exec boil.ContextExecutor, insert bool, related *RoutingTableSnapshot) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"provides\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"final_routing_table_id"}),
		strmangle.WhereClause("\"", "\"", 2, providePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.FinalRoutingTableID, related.ID)
	if o.R == nil {
		o.R = &provideR{
			FinalRoutingTable: related,
		}
	} else {
		o.R.FinalRoutingTable = related
	}

	if related.R == nil {
		related.R = &routingTableSnapshotR{
			FinalRoutingTableProvides: ProvideSlice{o},
		}
	} else {
		related.R.FinalRoutingTableProvides = append(related.R.FinalRoutingTableProvides, o)
	}

	return nil
}

// RemoveFinalRoutingTable relationship.
// Sets o.R.FinalRoutingTable to nil.
// Removes o from all passed in related items' relationships struct (Optional).
func (o *Provide) RemoveFinalRoutingTable(ctx context.Context, exec boil.ContextExecutor, related *RoutingTableSnapshot) error {
	var err error

	queries.SetScanner(&o.FinalRoutingTableID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("final_routing_table_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.FinalRoutingTable = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.FinalRoutingTableProvides {
		if queries.Equal(o.FinalRoutingTableID, ri.FinalRoutingTableID) {
			continue
		}

		ln := len(related.R.FinalRoutingTableProvides)
		if ln > 1 && i < ln-1 {
			related.R.FinalRoutingTableProvides[i] = related.R.FinalRoutingTableProvides[ln-1]
		}
		related.R.FinalRoutingTableProvides = related.R.FinalRoutingTableProvides[:ln-1]
		break
	}
	return nil
}

// SetInitialRoutingTable of the provide to the related item.
// Sets o.R.InitialRoutingTable to related.
// Adds o to related.R.InitialRoutingTableProvides.
func (o *Provide) SetInitialRoutingTable(ctx context.Context, exec boil.ContextExecutor, insert bool, related *RoutingTableSnapshot) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"provides\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"initial_routing_table_id"}),
		strmangle.WhereClause("\"", "\"", 2, providePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.InitialRoutingTableID = related.ID
	if o.R == nil {
		o.R = &provideR{
			InitialRoutingTable: related,
		}
	} else {
		o.R.InitialRoutingTable = related
	}

	if related.R == nil {
		related.R = &routingTableSnapshotR{
			InitialRoutingTableProvides: ProvideSlice{o},
		}
	} else {
		related.R.InitialRoutingTableProvides = append(related.R.InitialRoutingTableProvides, o)
	}

	return nil
}

// SetProvider of the provide to the related item.
// Sets o.R.Provider to related.
// Adds o to related.R.ProviderProvides.
func (o *Provide) SetProvider(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Peer) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"provides\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"provider_id"}),
		strmangle.WhereClause("\"", "\"", 2, providePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ProviderID = related.ID
	if o.R == nil {
		o.R = &provideR{
			Provider: related,
		}
	} else {
		o.R.Provider = related
	}

	if related.R == nil {
		related.R = &peerR{
			ProviderProvides: ProvideSlice{o},
		}
	} else {
		related.R.ProviderProvides = append(related.R.ProviderProvides, o)
	}

	return nil
}

// AddAddProviderRPCS adds the given related objects to the existing relationships
// of the provide, optionally inserting them as new records.
// Appends related to o.R.AddProviderRPCS.
// Sets related.R.Provides appropriately.
func (o *Provide) AddAddProviderRPCS(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*AddProviderRPC) error {
	var err error
	for _, rel := range related {
		if insert {
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		}
	}

	for _, rel := range related {
		query := "insert into \"provides_x_add_provider_rpcs\" (\"provide_id\", \"add_provider_rpc_id\") values ($1, $2)"
		values := []interface{}{o.ID, rel.ID}

		if boil.IsDebug(ctx) {
			writer := boil.DebugWriterFrom(ctx)
			fmt.Fprintln(writer, query)
			fmt.Fprintln(writer, values)
		}
		_, err = exec.ExecContext(ctx, query, values...)
		if err != nil {
			return errors.Wrap(err, "failed to insert into join table")
		}
	}
	if o.R == nil {
		o.R = &provideR{
			AddProviderRPCS: related,
		}
	} else {
		o.R.AddProviderRPCS = append(o.R.AddProviderRPCS, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &addProviderRPCR{
				Provides: ProvideSlice{o},
			}
		} else {
			rel.R.Provides = append(rel.R.Provides, o)
		}
	}
	return nil
}

// SetAddProviderRPCS removes all previously related items of the
// provide replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Provides's AddProviderRPCS accordingly.
// Replaces o.R.AddProviderRPCS with related.
// Sets related.R.Provides's AddProviderRPCS accordingly.
func (o *Provide) SetAddProviderRPCS(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*AddProviderRPC) error {
	query := "delete from \"provides_x_add_provider_rpcs\" where \"provide_id\" = $1"
	values := []interface{}{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	removeAddProviderRPCSFromProvidesSlice(o, related)
	if o.R != nil {
		o.R.AddProviderRPCS = nil
	}
	return o.AddAddProviderRPCS(ctx, exec, insert, related...)
}

// RemoveAddProviderRPCS relationships from objects passed in.
// Removes related items from R.AddProviderRPCS (uses pointer comparison, removal does not keep order)
// Sets related.R.Provides.
func (o *Provide) RemoveAddProviderRPCS(ctx context.Context, exec boil.ContextExecutor, related ...*AddProviderRPC) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	query := fmt.Sprintf(
		"delete from \"provides_x_add_provider_rpcs\" where \"provide_id\" = $1 and \"add_provider_rpc_id\" in (%s)",
		strmangle.Placeholders(dialect.UseIndexPlaceholders, len(related), 2, 1),
	)
	values := []interface{}{o.ID}
	for _, rel := range related {
		values = append(values, rel.ID)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err = exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}
	removeAddProviderRPCSFromProvidesSlice(o, related)
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.AddProviderRPCS {
			if rel != ri {
				continue
			}

			ln := len(o.R.AddProviderRPCS)
			if ln > 1 && i < ln-1 {
				o.R.AddProviderRPCS[i] = o.R.AddProviderRPCS[ln-1]
			}
			o.R.AddProviderRPCS = o.R.AddProviderRPCS[:ln-1]
			break
		}
	}

	return nil
}

func removeAddProviderRPCSFromProvidesSlice(o *Provide, related []*AddProviderRPC) {
	for _, rel := range related {
		if rel.R == nil {
			continue
		}
		for i, ri := range rel.R.Provides {
			if o.ID != ri.ID {
				continue
			}

			ln := len(rel.R.Provides)
			if ln > 1 && i < ln-1 {
				rel.R.Provides[i] = rel.R.Provides[ln-1]
			}
			rel.R.Provides = rel.R.Provides[:ln-1]
			break
		}
	}
}

// AddConnections adds the given related objects to the existing relationships
// of the provide, optionally inserting them as new records.
// Appends related to o.R.Connections.
// Sets related.R.Provides appropriately.
func (o *Provide) AddConnections(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Connection) error {
	var err error
	for _, rel := range related {
		if insert {
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		}
	}

	for _, rel := range related {
		query := "insert into \"provides_x_connections\" (\"provide_id\", \"connection_id\") values ($1, $2)"
		values := []interface{}{o.ID, rel.ID}

		if boil.IsDebug(ctx) {
			writer := boil.DebugWriterFrom(ctx)
			fmt.Fprintln(writer, query)
			fmt.Fprintln(writer, values)
		}
		_, err = exec.ExecContext(ctx, query, values...)
		if err != nil {
			return errors.Wrap(err, "failed to insert into join table")
		}
	}
	if o.R == nil {
		o.R = &provideR{
			Connections: related,
		}
	} else {
		o.R.Connections = append(o.R.Connections, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &connectionR{
				Provides: ProvideSlice{o},
			}
		} else {
			rel.R.Provides = append(rel.R.Provides, o)
		}
	}
	return nil
}

// SetConnections removes all previously related items of the
// provide replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Provides's Connections accordingly.
// Replaces o.R.Connections with related.
// Sets related.R.Provides's Connections accordingly.
func (o *Provide) SetConnections(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Connection) error {
	query := "delete from \"provides_x_connections\" where \"provide_id\" = $1"
	values := []interface{}{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	removeConnectionsFromProvidesSlice(o, related)
	if o.R != nil {
		o.R.Connections = nil
	}
	return o.AddConnections(ctx, exec, insert, related...)
}

// RemoveConnections relationships from objects passed in.
// Removes related items from R.Connections (uses pointer comparison, removal does not keep order)
// Sets related.R.Provides.
func (o *Provide) RemoveConnections(ctx context.Context, exec boil.ContextExecutor, related ...*Connection) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	query := fmt.Sprintf(
		"delete from \"provides_x_connections\" where \"provide_id\" = $1 and \"connection_id\" in (%s)",
		strmangle.Placeholders(dialect.UseIndexPlaceholders, len(related), 2, 1),
	)
	values := []interface{}{o.ID}
	for _, rel := range related {
		values = append(values, rel.ID)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err = exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}
	removeConnectionsFromProvidesSlice(o, related)
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.Connections {
			if rel != ri {
				continue
			}

			ln := len(o.R.Connections)
			if ln > 1 && i < ln-1 {
				o.R.Connections[i] = o.R.Connections[ln-1]
			}
			o.R.Connections = o.R.Connections[:ln-1]
			break
		}
	}

	return nil
}

func removeConnectionsFromProvidesSlice(o *Provide, related []*Connection) {
	for _, rel := range related {
		if rel.R == nil {
			continue
		}
		for i, ri := range rel.R.Provides {
			if o.ID != ri.ID {
				continue
			}

			ln := len(rel.R.Provides)
			if ln > 1 && i < ln-1 {
				rel.R.Provides[i] = rel.R.Provides[ln-1]
			}
			rel.R.Provides = rel.R.Provides[:ln-1]
			break
		}
	}
}

// AddDials adds the given related objects to the existing relationships
// of the provide, optionally inserting them as new records.
// Appends related to o.R.Dials.
// Sets related.R.Provides appropriately.
func (o *Provide) AddDials(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Dial) error {
	var err error
	for _, rel := range related {
		if insert {
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		}
	}

	for _, rel := range related {
		query := "insert into \"provides_x_dials\" (\"provide_id\", \"dial_id\") values ($1, $2)"
		values := []interface{}{o.ID, rel.ID}

		if boil.IsDebug(ctx) {
			writer := boil.DebugWriterFrom(ctx)
			fmt.Fprintln(writer, query)
			fmt.Fprintln(writer, values)
		}
		_, err = exec.ExecContext(ctx, query, values...)
		if err != nil {
			return errors.Wrap(err, "failed to insert into join table")
		}
	}
	if o.R == nil {
		o.R = &provideR{
			Dials: related,
		}
	} else {
		o.R.Dials = append(o.R.Dials, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &dialR{
				Provides: ProvideSlice{o},
			}
		} else {
			rel.R.Provides = append(rel.R.Provides, o)
		}
	}
	return nil
}

// SetDials removes all previously related items of the
// provide replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Provides's Dials accordingly.
// Replaces o.R.Dials with related.
// Sets related.R.Provides's Dials accordingly.
func (o *Provide) SetDials(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Dial) error {
	query := "delete from \"provides_x_dials\" where \"provide_id\" = $1"
	values := []interface{}{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	removeDialsFromProvidesSlice(o, related)
	if o.R != nil {
		o.R.Dials = nil
	}
	return o.AddDials(ctx, exec, insert, related...)
}

// RemoveDials relationships from objects passed in.
// Removes related items from R.Dials (uses pointer comparison, removal does not keep order)
// Sets related.R.Provides.
func (o *Provide) RemoveDials(ctx context.Context, exec boil.ContextExecutor, related ...*Dial) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	query := fmt.Sprintf(
		"delete from \"provides_x_dials\" where \"provide_id\" = $1 and \"dial_id\" in (%s)",
		strmangle.Placeholders(dialect.UseIndexPlaceholders, len(related), 2, 1),
	)
	values := []interface{}{o.ID}
	for _, rel := range related {
		values = append(values, rel.ID)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err = exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}
	removeDialsFromProvidesSlice(o, related)
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.Dials {
			if rel != ri {
				continue
			}

			ln := len(o.R.Dials)
			if ln > 1 && i < ln-1 {
				o.R.Dials[i] = o.R.Dials[ln-1]
			}
			o.R.Dials = o.R.Dials[:ln-1]
			break
		}
	}

	return nil
}

func removeDialsFromProvidesSlice(o *Provide, related []*Dial) {
	for _, rel := range related {
		if rel.R == nil {
			continue
		}
		for i, ri := range rel.R.Provides {
			if o.ID != ri.ID {
				continue
			}

			ln := len(rel.R.Provides)
			if ln > 1 && i < ln-1 {
				rel.R.Provides[i] = rel.R.Provides[ln-1]
			}
			rel.R.Provides = rel.R.Provides[:ln-1]
			break
		}
	}
}

// AddFindNodesRPCS adds the given related objects to the existing relationships
// of the provide, optionally inserting them as new records.
// Appends related to o.R.FindNodesRPCS.
// Sets related.R.Provides appropriately.
func (o *Provide) AddFindNodesRPCS(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*FindNodesRPC) error {
	var err error
	for _, rel := range related {
		if insert {
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		}
	}

	for _, rel := range related {
		query := "insert into \"provides_x_find_nodes_rpcs\" (\"provide_id\", \"find_nodes_rpc_id\") values ($1, $2)"
		values := []interface{}{o.ID, rel.ID}

		if boil.IsDebug(ctx) {
			writer := boil.DebugWriterFrom(ctx)
			fmt.Fprintln(writer, query)
			fmt.Fprintln(writer, values)
		}
		_, err = exec.ExecContext(ctx, query, values...)
		if err != nil {
			return errors.Wrap(err, "failed to insert into join table")
		}
	}
	if o.R == nil {
		o.R = &provideR{
			FindNodesRPCS: related,
		}
	} else {
		o.R.FindNodesRPCS = append(o.R.FindNodesRPCS, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &findNodesRPCR{
				Provides: ProvideSlice{o},
			}
		} else {
			rel.R.Provides = append(rel.R.Provides, o)
		}
	}
	return nil
}

// SetFindNodesRPCS removes all previously related items of the
// provide replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Provides's FindNodesRPCS accordingly.
// Replaces o.R.FindNodesRPCS with related.
// Sets related.R.Provides's FindNodesRPCS accordingly.
func (o *Provide) SetFindNodesRPCS(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*FindNodesRPC) error {
	query := "delete from \"provides_x_find_nodes_rpcs\" where \"provide_id\" = $1"
	values := []interface{}{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	removeFindNodesRPCSFromProvidesSlice(o, related)
	if o.R != nil {
		o.R.FindNodesRPCS = nil
	}
	return o.AddFindNodesRPCS(ctx, exec, insert, related...)
}

// RemoveFindNodesRPCS relationships from objects passed in.
// Removes related items from R.FindNodesRPCS (uses pointer comparison, removal does not keep order)
// Sets related.R.Provides.
func (o *Provide) RemoveFindNodesRPCS(ctx context.Context, exec boil.ContextExecutor, related ...*FindNodesRPC) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	query := fmt.Sprintf(
		"delete from \"provides_x_find_nodes_rpcs\" where \"provide_id\" = $1 and \"find_nodes_rpc_id\" in (%s)",
		strmangle.Placeholders(dialect.UseIndexPlaceholders, len(related), 2, 1),
	)
	values := []interface{}{o.ID}
	for _, rel := range related {
		values = append(values, rel.ID)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err = exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}
	removeFindNodesRPCSFromProvidesSlice(o, related)
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.FindNodesRPCS {
			if rel != ri {
				continue
			}

			ln := len(o.R.FindNodesRPCS)
			if ln > 1 && i < ln-1 {
				o.R.FindNodesRPCS[i] = o.R.FindNodesRPCS[ln-1]
			}
			o.R.FindNodesRPCS = o.R.FindNodesRPCS[:ln-1]
			break
		}
	}

	return nil
}

func removeFindNodesRPCSFromProvidesSlice(o *Provide, related []*FindNodesRPC) {
	for _, rel := range related {
		if rel.R == nil {
			continue
		}
		for i, ri := range rel.R.Provides {
			if o.ID != ri.ID {
				continue
			}

			ln := len(rel.R.Provides)
			if ln > 1 && i < ln-1 {
				rel.R.Provides[i] = rel.R.Provides[ln-1]
			}
			rel.R.Provides = rel.R.Provides[:ln-1]
			break
		}
	}
}

// AddPeerStates adds the given related objects to the existing relationships
// of the provide, optionally inserting them as new records.
// Appends related to o.R.PeerStates.
// Sets related.R.Provides appropriately.
func (o *Provide) AddPeerStates(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*PeerState) error {
	var err error
	for _, rel := range related {
		if insert {
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		}
	}

	for _, rel := range related {
		query := "insert into \"provides_x_peer_states\" (\"provide_id\", \"peer_state_id\") values ($1, $2)"
		values := []interface{}{o.ID, rel.ID}

		if boil.IsDebug(ctx) {
			writer := boil.DebugWriterFrom(ctx)
			fmt.Fprintln(writer, query)
			fmt.Fprintln(writer, values)
		}
		_, err = exec.ExecContext(ctx, query, values...)
		if err != nil {
			return errors.Wrap(err, "failed to insert into join table")
		}
	}
	if o.R == nil {
		o.R = &provideR{
			PeerStates: related,
		}
	} else {
		o.R.PeerStates = append(o.R.PeerStates, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &peerStateR{
				Provides: ProvideSlice{o},
			}
		} else {
			rel.R.Provides = append(rel.R.Provides, o)
		}
	}
	return nil
}

// SetPeerStates removes all previously related items of the
// provide replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Provides's PeerStates accordingly.
// Replaces o.R.PeerStates with related.
// Sets related.R.Provides's PeerStates accordingly.
func (o *Provide) SetPeerStates(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*PeerState) error {
	query := "delete from \"provides_x_peer_states\" where \"provide_id\" = $1"
	values := []interface{}{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	removePeerStatesFromProvidesSlice(o, related)
	if o.R != nil {
		o.R.PeerStates = nil
	}
	return o.AddPeerStates(ctx, exec, insert, related...)
}

// RemovePeerStates relationships from objects passed in.
// Removes related items from R.PeerStates (uses pointer comparison, removal does not keep order)
// Sets related.R.Provides.
func (o *Provide) RemovePeerStates(ctx context.Context, exec boil.ContextExecutor, related ...*PeerState) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	query := fmt.Sprintf(
		"delete from \"provides_x_peer_states\" where \"provide_id\" = $1 and \"peer_state_id\" in (%s)",
		strmangle.Placeholders(dialect.UseIndexPlaceholders, len(related), 2, 1),
	)
	values := []interface{}{o.ID}
	for _, rel := range related {
		values = append(values, rel.ID)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err = exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}
	removePeerStatesFromProvidesSlice(o, related)
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.PeerStates {
			if rel != ri {
				continue
			}

			ln := len(o.R.PeerStates)
			if ln > 1 && i < ln-1 {
				o.R.PeerStates[i] = o.R.PeerStates[ln-1]
			}
			o.R.PeerStates = o.R.PeerStates[:ln-1]
			break
		}
	}

	return nil
}

func removePeerStatesFromProvidesSlice(o *Provide, related []*PeerState) {
	for _, rel := range related {
		if rel.R == nil {
			continue
		}
		for i, ri := range rel.R.Provides {
			if o.ID != ri.ID {
				continue
			}

			ln := len(rel.R.Provides)
			if ln > 1 && i < ln-1 {
				rel.R.Provides[i] = rel.R.Provides[ln-1]
			}
			rel.R.Provides = rel.R.Provides[:ln-1]
			break
		}
	}
}

// Provides retrieves all the records using an executor.
func Provides(mods ...qm.QueryMod) provideQuery {
	mods = append(mods, qm.From("\"provides\""))
	return provideQuery{NewQuery(mods...)}
}

// FindProvide retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindProvide(ctx context.Context, exec boil.ContextExecutor, iD int, selectCols ...string) (*Provide, error) {
	provideObj := &Provide{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"provides\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, provideObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from provides")
	}

	if err = provideObj.doAfterSelectHooks(ctx, exec); err != nil {
		return provideObj, err
	}

	return provideObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *Provide) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no provides provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(provideColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	provideInsertCacheMut.RLock()
	cache, cached := provideInsertCache[key]
	provideInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			provideAllColumns,
			provideColumnsWithDefault,
			provideColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(provideType, provideMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(provideType, provideMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"provides\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"provides\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into provides")
	}

	if !cached {
		provideInsertCacheMut.Lock()
		provideInsertCache[key] = cache
		provideInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the Provide.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *Provide) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	provideUpdateCacheMut.RLock()
	cache, cached := provideUpdateCache[key]
	provideUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			provideAllColumns,
			providePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update provides, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"provides\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, providePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(provideType, provideMapping, append(wl, providePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update provides row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for provides")
	}

	if !cached {
		provideUpdateCacheMut.Lock()
		provideUpdateCache[key] = cache
		provideUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q provideQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for provides")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for provides")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o ProvideSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), providePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"provides\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, providePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in provide slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all provide")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *Provide) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no provides provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(provideColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	provideUpsertCacheMut.RLock()
	cache, cached := provideUpsertCache[key]
	provideUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			provideAllColumns,
			provideColumnsWithDefault,
			provideColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			provideAllColumns,
			providePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert provides, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(providePrimaryKeyColumns))
			copy(conflict, providePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"provides\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(provideType, provideMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(provideType, provideMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert provides")
	}

	if !cached {
		provideUpsertCacheMut.Lock()
		provideUpsertCache[key] = cache
		provideUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single Provide record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *Provide) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no Provide provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), providePrimaryKeyMapping)
	sql := "DELETE FROM \"provides\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from provides")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for provides")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q provideQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no provideQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from provides")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for provides")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o ProvideSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(provideBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), providePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"provides\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, providePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from provide slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for provides")
	}

	if len(provideAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *Provide) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindProvide(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ProvideSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := ProvideSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), providePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"provides\".* FROM \"provides\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, providePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in ProvideSlice")
	}

	*o = slice

	return nil
}

// ProvideExists checks if the Provide row exists.
func ProvideExists(ctx context.Context, exec boil.ContextExecutor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"provides\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if provides exists")
	}

	return exists, nil
}
